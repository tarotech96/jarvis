# node_modules rather than Yarn's default Plug'n'Play: Turbo shells out to
# `python3` and `node scripts/check.mjs` in each workspace, and PnP's
# resolver adds a layer neither of those needs.
nodeLinker: node-modules

enableTelemetry: false
